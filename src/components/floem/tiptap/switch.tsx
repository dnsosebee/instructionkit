import { NodeViewContent, NodeViewWrapper } from '@tiptap/react'

// THIS IS NOT CURRENTLY IN USE
const Switch = () => {
  return (
    <NodeViewWrapper>
      <NodeViewContent as='p' className='switch-content' />
    </NodeViewWrapper>
  )
}

export default Switch
