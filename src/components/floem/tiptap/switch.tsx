import { NodeViewContent, NodeViewWrapper } from '@tiptap/react'

const Switch = () => {
  return (
    <NodeViewWrapper className=' border border-purple-400'>
      <NodeViewContent className='flex' />
    </NodeViewWrapper>
  )
}

export default Switch
